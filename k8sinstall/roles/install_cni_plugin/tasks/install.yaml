---
- name: create directory
  file: 
    path: /opt/cni/bin
    state: directory

- name: unarchive cni_plugins
  unarchive:
    src: cni-plugins-linux-amd64-{{ cni_plugin_version }}.tgz
    dest: /opt/cni/bin
    remote_src: no
